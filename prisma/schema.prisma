generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Food {
  id          Int      @id @default(autoincrement())
  userId      String   // user_id
  name        String   // name
  quantity    String   // 個数
  buyDate     DateTime @default(now()) // buy_date (デフォルトは今日)
  trashDate   DateTime? // trash_date (空でもOK)
  createdAt   DateTime @default(now())
}